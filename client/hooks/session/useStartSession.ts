import { createSession } from "@/services/firebaseSessionService";
import { SessionData } from "@/types/types";

function startSession() {
    // createSession({
    //     session_id: "unique_session_id",
    //     start_time: 
    //     // start_time: new Date().toISOString(),
    //     status: "active",
    // });
}